# Design

<!-- impeccable:design-schema 1 -->

## World

现代亲和(Modern & Approachable):浅色底、蓝色主色、大圆角、柔和阴影的桌面工作台视觉语言。为"可控 / 可审计 / 本机运行"的 2B 产品服务,亲和而不失可信。用户于 2026-08-25 在可视化决策中选定,取代 pilot 当前的功能骨架视觉。

## Design Principles

- 亲和优先:大圆角、柔和阴影、充足留白,降低业务人员对"AI 工具"的戒备。
- 信任可见:本机 / 审计 / 模型状态常驻;风险用明确的语义色(红 / 橙 / 绿),不被装饰稀释。
- 结构同构:壳层(顶栏 / 左栏 / 状态栏 / 抽屉)全局一致,智能体表面在三种范式内变化。
- 状态即语义:运行中(蓝)、排队(橙)、完成(绿)、风险(红)、等待审批(琥珀)。

## Tokens

- 背景 `#F5F7FB`;表面 `#FFFFFF`;边框 `#EAF0F6` / `#E2E8F0`
- 文字 `#1E293B`(主)/ `#475569`(次)/ `#94A3B8`(弱)
- 主色 `#2563EB`;hover/active `#1D4ED8`
- 语义:风险红 `#B91C1C`(底 `#FEF2F2`)、等待橙 `#C2410C`(底 `#FFF7ED`)、完成绿 `#15803D`(底 `#F0FDF4`)、信息蓝 `#2563EB`(底 `#EFF6FF`)
- 圆角:卡片/面板 12px、按钮 10px、徽标 pill 999px、小控件 8px
- 阴影:卡片 `0 2px 8px rgba(15,23,42,.04)`;浮层(抽屉/模态)`0 8px 24px rgba(15,23,42,.12)`
- 字体:system-ui 栈(中文走系统字体);信息密度层 11–14px;数字可用 tabular-nums
- 间距刻度:4 / 8 / 12 / 16

## Component Character

- 卡片式面板:圆角 12px、细边框、浅阴影,不用重投影。
- 按钮:主按钮蓝底白字、圆角 10px;次按钮白底细边框。
- 状态徽标:pill 形、浅底深字。
- 抽屉/模态:圆角 14px、柔和阴影,进入动画 160–220ms ease-out。
- 流程条:步骤圆点 + 连接线;进行中蓝色、完成绿勾、失败红叉、等待灰。

## Shell

- 顶栏:应用身份、当前智能体、本机/审计标记、审批角标。
- 左栏:已安装智能体 + 状态点/排队徽标;底部审批 / 审计入口。
- 表面头部:智能体/会话名、会话抽屉按钮(▾)、新会话 / 暂停 / 打断。
- 底部状态栏:一句话状态 + "运行 3/4 · 2 排队"(点击展开队列面板)+ 技术信息 hover 展开。
- 全局:审批中心(抽屉)、审计时间线、Ctrl+K 命令面板。

## Surfaces(三种智能体表面)

1. 任务流式(合同审核):流程条 + 左报告/原文页签 + 右风险发现(W1);复核与导出(走审批);活动条。
2. 对话式(法规问答):消息流 + 内联工具卡片可展开(C1);Composer(发送/停止三态、附件)。
3. 仪表板式(舆情监控):指标卡行 → 主趋势图 → 明细表(D1);时间范围 + 刷新/定时。

## Approval Ritual

- 默认右侧面板(P2),上下文可见;高风险升级居中模态(P1)+ 二次确认;全局审批中心(P3)常驻。
- 每项包含:操作描述、目标、风险、倒计时、可展开的冻结参数、审批意见、拒绝/批准。

## Audit Trail

- T1 时间线为默认视图,被拒绝的操作明确标注"未执行";筛选(智能体 / 事件类型 / 结果)+ 导出。
- T2 表格视图用于内部查看与导出。

## Motion

- 状态流转、审批弹出、抽屉滑入用轻量动画(160–220ms)。
- 流式生成时输入光标呼吸;活动条实时更新,不做无关装饰动画。

## Notes

- 深色模式暂不做,列为后续主题扩展(V2 深色工程风可作为备选主题)。
- 本文件是方向契约,最终以可交互原型与真实构建为准做校准。
